<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Mis Vacantes</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <%- include('partials/navbar') %>
  <div class="container-fluid mt-4">
    <div class="row">
      <%- include('partials/sidebar') %>
      <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
        <h2 class="mb-4">Mis Vacantes</h2>

        <% if (vacancies.length > 0) { %>
          <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
            <% vacancies.forEach(vacancy => { %>
              <div class="col">
                <div class="card h-100">
                  <div class="card-body d-flex flex-column">
                    <h5 class="card-title"><%= vacancy.title %></h5>
                    <p class="card-text mb-1"><strong>Descripción:</strong> <%= vacancy.description %></p>
                    <p class="card-text mb-1"><strong>Prom. Mínimo:</strong> <%= vacancy.minAverage %></p>
                    <p class="card-text mb-1"><strong>Carrera:</strong> <%= vacancy.requiredCareer %></p>
                    <p class="card-text mb-3"><strong>Trimestres Mínimos:</strong> <%= vacancy.minQuarters %></p>
                    <div class="mt-auto d-flex justify-content-between">
                      <a 
                        href="/company-vacancies/<%= vacancy._id %>/edit" 
                        class="btn btn-sm btn-primary"
                      >
                        Editar
                      </a>
                      <form 
                        action="/company-vacancies/<%= vacancy._id %>/delete" 
                        method="POST" 
                        onsubmit="return confirm('¿Seguro que deseas eliminar esta vacante?');"
                      >
                        <button type="submit" class="btn btn-sm btn-danger">
                          Eliminar
                        </button>
                      </form>
                    </div>
                  </div>
                </div>
              </div>
            <% }); %>
          </div>
        <% } else { %>
          <div class="alert alert-info">
            No has publicado ninguna vacante. <a href="/company-post-vacancy">Publica una ahora</a>.
          </div>
        <% } %>
      </main>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
