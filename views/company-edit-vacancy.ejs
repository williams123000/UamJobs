<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>

  <%- include('partials/navbar') %>
  <br><br><br>
  <div class="container-fluid">
      <div class="row">
          <%- include('partials/sidebar') %>
          <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4 order-md-last order-first">
<div class="container mt-4">
  <h1><%= title %></h1>
  <form action="/company-vacancies/<%= vacancy._id %>/edit" method="POST">
    <div class="mb-3">
      <label for="title" class="form-label">Título</label>
      <input 
        type="text" 
        id="title" 
        name="title" 
        class="form-control" 
        value="<%= vacancy.title %>" 
        required
      >
    </div>
    <div class="mb-3">
      <label for="description" class="form-label">Descripción</label>
      <textarea 
        id="description" 
        name="description" 
        class="form-control" 
        rows="4" 
        required
      ><%= vacancy.description %></textarea>
    </div>
    <!-- Repite para minGPA, career y minTerms -->
    <div class="mb-3">
      <label for="minAverage" class="form-label">Promedio mínimo</label>
      <input 
        type="number" 
        step="0.1" 
        id="minAverage" 
        name="minAverage" 
        class="form-control" 
        value="<%= vacancy.minAverage %>"
      >
    </div>
    <div class="mb-3">
      <label for="requiredCareer" class="form-label">Carrera</label>
      <select id="requiredCareer" name="requiredCareer" class="form-select">
        <% ['Ingeniería','Medecina','Derecho', 'Administración', 'Arquitectura', 'Psicología', 'Contabilidad'].forEach(c => { %>
          <option value="<%= c %>" <%= c===vacancy.requiredCareer?'selected':'' %>><%= c %></option>
        <% }) %>
      </select>
    </div>
    <div class="mb-3">
      <label for="minQuarters" class="form-label">Trimestres mínimos</label>
      <input 
        type="number"
        id="minQuarters"
        name="minQuarters"
        class="form-control" 
        value="<%= vacancy.minQuarters %>"
      >
    </div>
    <button type="submit" class="btn btn-success">Guardar cambios</button>
    <a href="/company-vacancies" class="btn btn-secondary ms-2">Cancelar</a>
  </form>
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
